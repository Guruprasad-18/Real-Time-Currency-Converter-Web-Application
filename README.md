# Real-Time Currency Converter Web Application

A simple and interactive currency converter web application that allows users to convert values between multiple international currencies using live exchange rate data fetched from a public web API.

---

## 🚀 Features

- Convert currencies using **real-time exchange rates**
- Dynamic **currency selection dropdowns**
- Automatic **country flag updates** based on selected currency
- **Input validation and error handling** for reliable conversions
- Clean and responsive user interface

---

## 🛠 Tech Stack

- **HTML** – Structure of the application  
- **CSS** – Styling and responsive layout using Flexbox  
- **JavaScript** – Application logic, DOM manipulation, and API handling  
- **Web API** – Fetching live currency exchange rates  

---

## ⚙️ How It Works

1. User enters an amount and selects source and target currencies.
2. The application fetches the latest exchange rates using a public web API.
3. Conversion is calculated dynamically and displayed in real time.
4. Currency flags and results update automatically based on user selection.

---

## 📂 Project Structure

```text
├── index.html     # Application structure
├── style.css      # Styling and layout
├── app.js         # Main application logic
├── codes.js       # Currency-to-country code mapping
